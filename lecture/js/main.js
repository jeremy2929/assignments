// Importing all dependent code (libs)
// The "main" or "entry" file should consist of
// only the things to "kick off" our code
var $ = require("jquery");
var albums = require("./album");  // inlcuding to link in album.js file
var modal = require("./modal");
